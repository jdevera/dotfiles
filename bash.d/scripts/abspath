# # TODO: convert to script
# Obtains the absolute path for any file or directory
#
getabspath()
{
if [[ ! -e $1 ]]
then
   echo "$1 does not exist"
   return 1
fi
 
echo `cd \`dirname $1\`; pwd`/`basename $1`
}
